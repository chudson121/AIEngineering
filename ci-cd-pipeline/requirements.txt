# Core LLM providers
openai>=1.30.0
anthropic>=0.26.0
boto3>=1.34.0                        # AWS Bedrock + S3
azure-ai-ml>=1.16.0                  # Azure ML / OpenAI
azure-storage-blob>=12.20.0          # Azure Blob Storage

# LangChain + providers
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0
langchain-community>=0.2.0
langchain-core>=0.2.0

# Evaluation frameworks
ragas>=0.1.9                         # Retrieval & groundedness (RAGAS)
deepeval>=0.21.0                     # Hallucination, faithfulness (DeepEval)
evidently>=0.4.30                    # Drift detection, model comparison

# Observability & tracing
langsmith>=0.1.75                    # LangSmith tracing + dataset registry
langfuse>=2.36.0                     # Production trace monitoring
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp>=1.24.0

# Experiment tracking & model registry
mlflow>=2.13.0

# Dataset versioning
dvc[azure,s3]>=3.51.0

# Runtime & serving
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0

# Alerting
pagerduty-api-client>=2.0.0
requests>=2.32.0

# Utilities
pyyaml>=6.0.1
rich>=13.7.1
pandas>=2.2.0
datasets>=2.20.0                     # HuggingFace datasets (RAGAS dependency)
